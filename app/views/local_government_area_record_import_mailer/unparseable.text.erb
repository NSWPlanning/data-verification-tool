Hi <%= @user.name %>,

Unfortunately, your import has failed.

Filename: <%= @filename %>

The file could not be successfully read. 

Some common errors:
- Have you forgotten to add a header row?
- Have you used | as the delimiter? 
- A trailing | at the end of a row is invalid.

Cheers,
The ePlanning Team.
<%= @host_name %>
